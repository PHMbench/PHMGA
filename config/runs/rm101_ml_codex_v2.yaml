# @package _global_
defaults:
  - override /data: rm101
  - override /experiment: ml
  - _self_

llm:
  mode: provider
  provider: codex_cli
  model: gpt-5.3-codex

runtime:
  workflow_mode: rich
  max_iterations: 4
  min_depth: 3
  min_width: 1
  max_depth: 8
  output_dir: /home/user/LQ/B_Signal/PHMGA/artifacts/paper/rm101_ml_codex_v2
