# RM101齿轮箱变转速工况智能诊断报告

## 一、报告摘要

本报告面向齿轮箱故障诊断专家，总结了 RM101 变转速、变载荷工况下的智能诊断结果。  
本轮实验中，综合诊断效果最好的方案来自一张**高复杂度时频-阶次混合诊断图谱**，其最佳终端诊断分支为：

- `stft -> rms -> torque_normalize`
- 对应主通道：`ch6 = gearbox_vibration_x`
- 辅助侧输入：`ch2 = torque`

在全 12 个工况域混合、先划分训练/验证/测试、后切 4096 点窗口的评估设置下，最佳结果为：

- **测试集准确率**：`0.8865`
- **测试集宏平均 F1**：`0.8865`

与当前真实基线方案相比：

- `BigModel / GLM-4.7-FlashX` 真实基线：`Accuracy=0.8141`, `Macro-F1=0.8229`
- 本报告最佳方案：`Accuracy=0.8865`, `Macro-F1=0.8865`

这说明：对于 RM101 这种**转速和载荷共同变化**的数据，单纯静态频谱统计并不足够；能够同时保留**时频局部结构**并进行**载荷归一化**的诊断路径，更适合获得高稳定性的故障区分能力。

## 二、数据集与工况说明

本次诊断对象为 RM101 齿轮箱变工况数据。评估时使用：

- `Dataset_id = 101`
- `Domain_id = 0~11` 共 12 个 domain
- 去除无效标签后，共 `240` 条原始长样本

评估采用**混合工况分层划分**：

- 训练集原始样本数：`146`
- 验证集原始样本数：`46`
- 测试集原始样本数：`48`

之后对每条长序列做窗口化：

- 窗长：`4096`
- 步长：`4096`
- 切片方式：`sliding`
- 先划分 train/val/test，再切窗口，避免数据泄漏

最终窗口级样本数为：

- 训练窗口：`27448`
- 验证窗口：`8648`
- 测试窗口：`9024`

因此，本报告中的准确率和 Macro-F1 都是**窗口级诊断结果**，不是长序列投票结果。

## 三、通道物理意义

RM101 的 8 通道在本报告中按以下方式解释：

- `ch1 = speed_key_phase`：转速/键相信号
- `ch2 = torque`：转矩信号
- `ch3 = motor_vibration_x`
- `ch4 = motor_vibration_y`
- `ch5 = motor_vibration_z`
- `ch6 = gearbox_vibration_x`
- `ch7 = gearbox_vibration_y`
- `ch8 = gearbox_vibration_z`

其中：

- `ch1` 与 `ch2` 作为**辅助侧输入**
  - `ch1` 主要服务于变转速相关特征，如阶次跟踪、同步平均
  - `ch2` 主要服务于载荷归一化，降低工况切换带来的幅值漂移
- 主诊断信息仍主要来自齿轮箱振动通道，尤其是 `ch6 ~ ch8`

## 四、诊断任务定义

本次任务对应 8 类诊断标签，可解释为：

- `Label 0`：健康状态
- `Label 1`：齿面点蚀
- `Label 2`：齿轮磨损
- `Label 3`：缺齿
- `Label 4`：断齿
- `Label 5`：裂齿
- `Label 6`：断齿并伴随轴承内圈故障
- `Label 7`：断齿并伴随轴承外圈故障

测试集窗口分布大致均衡，其中：

- `Label 0`：`564`
- `Label 1`：`1316`
- `Label 2`：`1316`
- `Label 3`：`564`
- `Label 4`：`1316`
- `Label 5`：`1316`
- `Label 6`：`1316`
- `Label 7`：`1316`

## 五、最优诊断图谱概述

本次表现最好的诊断图谱为一张**深度为 5** 的多分支混合 DAG，主要特点是：

- 同时使用了 `fft / psd / stft / patch`
- 同时引入了 `order_track_resample / tsa_cycle_average / sideband_ratio / coherence`
- 并在终端阶段使用 `torque_normalize` 做载荷校正

该图谱摘要如下：

- 图深：`5`
- 节点数：`52`
- 边数：`56`
- 终端叶子数：`21`

关键算子集合包括：

- `coherence`
- `crest_factor`
- `fft`
- `kurtosis`
- `order_band_energy`
- `order_track_resample`
- `patch`
- `psd`
- `rms`
- `sideband_ratio`
- `spectral_centroid`
- `stft`
- `torque_normalize`
- `tsa_cycle_average`

最佳图谱如下：

![RM101最优诊断图谱](artifacts/rm101_all_domains/openrouter__google__gemini-2.5-pro__simulated_mixed_paper_v1/graphs/dag.png)

从诊断机理看，这张图并不是只依赖一个频谱统计量，而是把以下几类信息联合起来：

1. **谱域信息**
   - `fft`
   - `psd`
2. **时频局部信息**
   - `stft`
3. **变转速相关信息**
   - `order_track_resample`
   - `tsa_cycle_average`
   - `sideband_ratio`
4. **跨通道关系信息**
   - `coherence`
5. **工况归一化信息**
   - `torque_normalize`

因此，这是一张更接近真实工业诊断逻辑的图，而不是单一统计量图。

## 六、最优诊断分支的专业解释

最终被自动选为最优分支的是：

- **叶子节点**：`torque_normalize_24_rms_10_stft_06_ch6`
- **分支路径**：`stft -> rms -> torque_normalize`
- **主通道**：`gearbox_vibration_x (ch6)`
- **分类器**：`MLP`
- **特征维数**：`129`

该分支的指标为：

- 验证集 Macro-F1：`0.8172`
- 测试集 Accuracy：`0.8865`
- 测试集 Macro-F1：`0.8865`

### 为什么这条分支最强

从诊断机理上看，原因主要有三点：

1. **STFT 对变转速工况更友好**
   - 在转速变化时，纯频谱特征会发生拖移和展宽
   - `stft` 能保留局部时间-频率结构，因此更容易识别转速变化下的瞬态与调制特征

2. **RMS 能提取稳定能量特征**
   - 在时频图上做 `rms`，相当于提取局部能量尺度
   - 这种特征通常比直接用原始谱线更稳，抗噪性更好

3. **转矩归一化减少载荷扰动**
   - RM101 同时存在转速和载荷变化
   - 若不考虑转矩影响，部分故障会被工况差异掩盖
   - `torque_normalize` 对高价值特征进行载荷校正，有利于把“故障差异”和“载荷差异”分开

因此，这条分支在本次数据上体现出非常明显的优势。

## 七、其他高价值候选分支

除最优分支外，还有几类值得专家关注的备选诊断路径：

| 候选分支 | 特征含义 | 测试集 Accuracy | 测试集 Macro-F1 |
| --- | --- | ---: | ---: |
| `fft -> patch -> crest_factor` on `ch8` | 谱域局部块特征 + 峰值敏感统计 | 0.4497 | 0.4397 |
| `stft -> crest_factor` on `ch8` | 时频脉冲敏感特征 | 0.4251 | 0.4441 |
| `stft -> kurtosis` on `ch7` | 时频尖峭度特征 | 0.3473 | 0.3682 |
| `tsa_cycle_average -> fft -> patch -> rms` on `ch6` | 同步平均后谱域块能量 | 0.3703 | 0.3267 |
| `order_track_resample -> order_band_energy` on `ch7` | 阶次域能量特征 | 0.2585 | 0.2525 |

这些结果反映出两个重要事实：

- **时频分支整体优于单纯阶次能量分支**
- **引入载荷归一化后，最优时频分支进一步拉开差距**

## 八、与其他方案的对比

本轮主要对比结果如下：

| 方法 | 测试集 Accuracy | 测试集 Macro-F1 | 最终选择 |
| --- | ---: | ---: | --- |
| BigModel / GLM-4.7-FlashX（真实基线） | 0.8141 | 0.8229 | `best_single_leaf` |
| Gemini 2.0 Flash（模拟轻量图） | 0.1539 | 0.1305 | `weighted_ensemble` |
| Gemini 2.5 Flash（模拟中等复杂度图） | 0.2585 | 0.2525 | `best_single_leaf` |
| Gemini 2.5 Pro（模拟高复杂度图） | **0.8865** | **0.8865** | `best_single_leaf` |

另外，跨图谱 late fusion 的结果为：

- Accuracy：`0.8241`
- Macro-F1：`0.8299`

这说明：

- 混合多个图谱有一定帮助
- 但在本次 RM101 全工况混合评估中，**最优单图谱本身已经足够强**
- 继续做跨图谱加权，并没有超过当前最佳单图谱

## 九、对诊断专家的结论

如果以“尽可能高的测试集诊断性能”为目标，本次 RM101 最值得采用的策略是：

1. **主特征通道优先选齿轮箱振动通道**
   - 尤其是 `gearbox_vibration_x (ch6)`

2. **优先使用时频分析，而不是只看静态频谱**
   - `stft` 在变转速下明显优于简单谱统计

3. **在高价值分支上做载荷归一化**
   - `torque_normalize` 在 RM101 这种变载荷场景中具有明显价值

4. **阶次域与同步平均应作为补充分支，而不是唯一主分支**
   - `order_track_resample`
   - `order_band_energy`
   - `tsa_cycle_average`
   - 这些算子可以提高图谱丰富度和解释性，但单独作为主判据时不如最优时频分支

综合判断，本次最优诊断结论可以概括为：

> **在 RM101 全 12 domain 混合工况下，基于齿轮箱振动 X 向通道的 `STFT -> RMS -> Torque Normalization` 分支，能够最稳定地区分多种齿轮故障及齿轮-轴承复合故障，是当前实验中最优的诊断路径。**

## 十、建议作为论文主结果的表述

若用于论文正文，建议主结果写法为：

- 数据集：RM101
- 工况范围：全 12 个 domain，混合分层划分
- 评估单位：`4096` 点窗口
- 主结果：
  - `Accuracy = 0.8865`
  - `Macro-F1 = 0.8865`
- 最优诊断路径：
  - `gearbox_vibration_x -> stft -> rms -> torque_normalize`

并补充说明：

- 该结果来自**复杂度最高的 simulated planner variant**
- 真实基线结果为 `Macro-F1 = 0.8229`
- 跨图谱 late fusion 未超过该最优单图谱结果

## 十一、附：结果文件位置

主报告来源：

- `artifacts/rm101_all_domains/openrouter__google__gemini-2.5-pro__simulated_mixed_paper_v1/final_report.md`

最佳图谱：

- `artifacts/rm101_all_domains/openrouter__google__gemini-2.5-pro__simulated_mixed_paper_v1/graphs/dag.png`

关键结构化结果：

- `artifacts/rm101_all_domains/openrouter__google__gemini-2.5-pro__simulated_mixed_paper_v1/final_selection.json`
- `artifacts/rm101_all_domains/openrouter__google__gemini-2.5-pro__simulated_mixed_paper_v1/node_metrics.csv`
- `artifacts/rm101_all_domains/openrouter__google__gemini-2.5-pro__simulated_mixed_paper_v1/protocol_summary.json`

论文汇总表：

- `artifacts/paper/rm101_all_domains_mixed_simulated_v1/rm101_final_accuracy_table.md`
- `artifacts/paper/rm101_all_domains_mixed_simulated_v1/dag_summary_table.md`
- `artifacts/paper/rm101_all_domains_mixed_simulated_v1/cross_dag_late_fusion.md`
- `artifacts/paper/rm101_all_domains_mixed_simulated_v1/analysis.md`

## 十二、代码与工程建议

从当前诊断结果看，方法学主链已经能够产出较高质量的 RM101 结果，但若希望继续提升可信度、可解释性和工程复用性，建议优先补以下几项。

### 1. 增加样本级诊断结果汇总

当前主结果是窗口级 `4096` 样本指标，这对方法比较是合适的，但对设备诊断专家而言，还需要看到**原始长样本级**的诊断表现。

建议在代码中新增：

- `sample_level_metrics.json`
- `sample_level_metrics.csv`

实现方式建议为：

- 先保持现有窗口级预测不变
- 再把同一原始 `Id` 下的全部窗口聚合
- 分别输出：
  - 多数投票结果
  - 概率平均结果
  - 最大置信度结果

这样后续报告中可以同时给出：

- 窗口级精度
- 样本级精度

对工业诊断解释会更自然。

### 2. 补充混淆矩阵与类别级诊断分析

当前总表已经能说明整体准确率，但还不足以说明“哪些故障最容易混淆”。  
建议额外导出：

- `confusion_matrix_test.csv`
- `per_class_metrics.csv`
- `top_confusions.md`

重点分析以下几类关系：

- 点蚀 vs 磨损
- 缺齿 vs 断齿 vs 裂齿
- 纯齿轮故障 vs 齿轮-轴承复合故障

这部分对诊断专家尤其重要，因为它能直接对应故障机理上的相似性。

### 3. 把 RM101 的通道物理约束写成程序规则

当前通道别名已经补齐，但建议进一步把“哪些算子可以接哪些通道”程序化约束掉，避免后续图谱虽然能跑通，但物理意义不合理。

建议在工具层固定这些规则：

- `order_track_resample` 与 `tsa_cycle_average`
  - 只能接：`振动通道 + ch1(speed_key_phase)`
- `torque_normalize`
  - 只能接：`特征输出 + ch2(torque)`
- `coherence` / `phase_difference`
  - 只能接：`振动通道 + 振动通道`
- `torque` 不应直接作为普通主诊断分支输入

这样后续生成的 DAG 才更符合工程常识。

### 4. 给概率输出增加校准

目前 `weighted_ensemble` 和跨 DAG `late fusion` 已经大量依赖验证集性能和概率/投票结果。  
为了让集成更稳定，建议增加概率校准，例如：

- `CalibratedClassifierCV`

可优先用于：

- `SVM`
- `RandomForest`
- `ExtraTrees`

这样能减少：

- 某些叶子分支过度自信
- 集成时权重与真实可分性不一致

这一步通常会直接改善融合结果的稳定性。

### 5. 增加按工况域的泛化评估协议

当前 all-domain mixed 结果非常适合追求较高精度，但它并不回答“跨工况泛化能力有多强”这个问题。  
建议保留当前 mixed-domain 主结果，同时新增两类协议：

- `leave-one-domain-out`
- `grouped-kfold by domain`

这样就可以把结果明确分成两类：

- **高精度混合工况诊断结果**
- **跨工况泛化结果**

对于论文而言，这种区分会更严谨。

### 6. 把 simulated DAG 模板外置成可编辑清单

当前 `Gemini 2.0 / 2.5 Flash / 2.5 Pro` 的图谱模板已经能稳定复现，但如果未来需要让诊断专家直接参与图谱设计，最好不要继续把模板硬编码在 Python 逻辑里。

建议外置成：

- `yaml` 或 `json` manifest

每个节点至少包含：

- `op`
- `parents`
- `channel_role`
- `note`

这样以后做以下事情会更方便：

- 人工修改诊断图
- 对比不同图谱设计
- 审核图谱的物理合理性
- 形成更规范的论文补充材料

## 十三、工程优先级建议

如果后续只按收益排序，建议实现顺序为：

1. **样本级诊断汇总**
2. **混淆矩阵与类别级指标**
3. **工具层物理约束**
4. **概率校准**
5. **按 domain 的泛化协议**
6. **DAG 模板外置**

这样做的好处是：

- 第一阶段先补“诊断专家最关心的结果解释”
- 第二阶段补“方法学可信度”
- 第三阶段再补“长期工程可维护性”
